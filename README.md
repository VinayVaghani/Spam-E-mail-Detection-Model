# Spam-E-mail-Detection-Model
Spam Email Detector built with Streamlit and Scikit-learn. Uses a notebook-trained model (CountVectorizer + MultinomialNB) saved as a .pkl file for fast prediction. Supports single email input via text paste or file upload (.txt/.eml).
